# シフトシステム 開発履歴・現状記録

最終更新: 2026-06-06 / 進行中のプロジェクト

---

## 基本情報

### URL一覧

| 種別 | URL |
|---|---|
| 公開URL | https://MARUNAGE-creator.github.io/shift-system/ |
| GitHubリポジトリ | https://github.com/MARUNAGE-creator/shift-system |
| アップロード用 | https://github.com/MARUNAGE-creator/shift-system/upload/main |
| Firebaseコンソール | https://console.firebase.google.com/ |

### GitHub

- ユーザー名: **MARUNAGE-creator**（職場でプライベートアドレスを使わないために変更した経緯あり）
- リポジトリ名: **shift-system**（Public）
- ホスティング: GitHub Pages（main branch）

### Firebase設定

| 項目 | 値 |
|---|---|
| projectId | shift-system-76e36 |
| apiKey | AIzaSyCARk-5Y1UTbOdauQffUNobx82wbR68jcA |
| リージョン | asia-northeast1 (Tokyo) |
| Firestoreモード | 当初テストモード → 現在 `allow read, write: if true;` で運用 |
| config保管場所 | `index.html` 内に直接記述 |

### 認証

- **管理者パスワード: `mansou0102`**

### Firestoreコレクション

| コレクション | 役割 |
|---|---|
| `staffList` | スタッフ一覧（時給、sortOrder等を含む） |
| `shiftReqs` | スタッフから提出されたシフト希望データ |
| `monthSubs` | 月別の提出状況管理 |
| `settings/global` | 締め切り設定（ON/OFF、日付、メッセージ） |
| `salaryCalcs` | 月別の給与計算結果保存（`{ym}`単位） |

### 技術スタック

- **フロントエンド**: vanilla JS（フレームワーク不使用、`index.html` 単一ファイル構成）
- **バックエンド/DB**: Firebase Firestore
- **ホスティング**: GitHub Pages
- **Excel出力**: **xlsx-js-style**（標準xlsxはスタイル書き込み不可のため切替）
  - 出力オプション: `bookSST: true` / `compression: true`（文字化け対策）
- **Excel入力**: .xlsm / .xlsx テンプレート読み込み対応

### データの保管・同期の仕組み

- 当初は localStorage（端末内保存）→ 共有不可問題のため **Firebase Firestoreへ全面移行**
- スタッフの希望シフト・スタッフ情報・設定・給与計算結果はすべてFirestoreに保存
- リアルタイム同期により複数端末・複数ユーザー間でデータ共有
- 過去の給与計算データは月別（`salaryCalcs/{ym}`）に永続保存
- スタッフ並び替えは長押しドラッグで自動的にFirestoreの `sortOrder` に反映

---

## 実装機能（現時点で稼働中）

### スタッフ画面

#### ログイン
- スタッフ名選択方式でログイン
- ログイン画面上部に締め切りバナーを常時表示
- 締め切り日超過時はログインをブロック（カスタムメッセージ表示、翌月1日に自動リセット）

#### カレンダー
- 月間カレンダー表示。各マス内に「9:30 / 〜18:30」の2行で時間表示
- 凡例はカラフルチップで視認性を確保
- 下部ナビゲーションタブはアクティブ時に青背景＋上部青ライン
- 画面上部に締め切りバナー（残日数によって色変化／点滅）
- **時間帯未選択時はカレンダーを非表示**（プレースホルダー表示）。「確認・提出」タブは🔒ロック・グレーアウト

#### 日付ポップアップ
- 日付タップで大きなポップアップを表示
- ボタン構成：「希望休」「時間変更」「シフト割り当て（朝/昼/夜/その他）」「🌴 有給休暇にする」「解除」
- 有給休暇カラー：紫(#9F7AEA)、セル背景は薄紫
- 枠外タップで閉じる

#### 時間ダイヤル
- 朝/昼/夜/その他いずれのモードでも「もう一度タップで時間ダイヤルが開く」仕様
- 9〜23時の範囲、15分刻みで指定可能

#### 提出フロー
- 提出ボタンは赤色・大型・パルスアニメ・「▼ 入力が終わったらここをタップ ▼」案内
- 時間帯未選択で提出ボタン押下 → 警告＋シフトボタン赤点滅
- 提出確認画面に進む

#### メモ
- 提出確認画面に「希望理由・特記事項」メモ欄（任意入力）
- 管理者画面でもメモが表示される

#### 締め切りバナー
- ログイン画面・カレンダー画面の両方に表示
- 「【5月15日(木)】まで」のように曜日まで併記する形式
- 残日数で色変化：
  - 6日以上：青「📅 今月の提出締め切り: 5月15日まで（あと8日）」
  - 3〜5日：黄
  - 1〜2日／当日：赤＋点滅

### 管理者画面（5タブ構成）

| タブ | 主な機能 |
|---|---|
| スタッフ管理 | スタッフ追加・編集（時給フィールド含む）、長押しドラッグ並び替え |
| シフト希望 | 各スタッフから提出された希望シフト・メモの閲覧 |
| データ出力 | 月別Excelエクスポート、既存Excelテンプレートへの反映、自動色付け |
| 給与計算 | Excel再アップロードからの自動集計、内訳付き表示、月別保存 |
| 設定 | 締め切りON/OFF・日付（1〜28）・カスタムメッセージ・プレビュー |

#### スタッフ管理＋並び替え
- スタッフ追加・編集フォームに「時給」フィールドを内包
- **0.4秒長押しでドラッグモード起動**（バイブ反応）
- 並び替え結果はFirestoreの `sortOrder` に自動保存

#### シフト希望
- 各スタッフの提出済み希望（時間帯、希望休、有給、時間変更、その他）を一覧表示
- 提出時のメモ（希望理由・特記事項）も併せて表示

#### データ出力
- 月別Excelエクスポート
- 既存Excelテンプレートに反映する機能
- 月セレクタ下のステータスバッジで、データの有無を一目で判別
  - 「✅ 提出済み 12名」「⚪ まだありません」
  - 「✅ 反映できる希望データ 12名分あり」
  - 「✅ 計算済み（35名・6/13更新）」

#### 給与計算（管理者のみ）
- Excel(.xlsm/.xlsx)アップロード方式で確定シフトを読み込み
- 各スタッフの労働時間を自動集計
- 全体合計と、スタッフ別カード（時給／労働時間／内訳3行／合計）を表示
- 月別にFirestore `salaryCalcs/{ym}` へ保存

#### 設定
- 締め切り機能のON/OFF切り替え
- 締め切り日（1〜28日）、カスタムメッセージ、プレビュー
- 翌月1日に自動リセット
- データ：Firestore `settings/global` ドキュメント

### 給与計算の計算ルール

- **基本給** = 時給 × 労働時間
- **休憩控除**：労働時間が6時間以上の場合に1時間固定で控除。5.5時間までは休憩控除なし
- **残業手当 +25%**：1日8時間超過分に対して時給×1.25
- **深夜手当 +25%**：22:00〜24:00 の時間帯に対して時給×1.25
- 残業＋深夜が重なる場合は **+50%**
- 締め日はカレンダー月単位
- 祝日は通常扱い、交通費・役職手当なし
- 時給未入力スタッフは0円表示
- 検証済みテストケース：5.5h／6h／夜勤9h／13h残業 など
- Phase 2残タスク：週40時間判定（木〜水締め）、法定休日割増+35%

### Excel連携

#### テンプレート反映
- 既存「新甘木勤怠5月.xlsm」形式の月間勤怠表にデータを書き戻し
- スタッフ名で照合（半角／全角空白も自動正規化）
- シフト種別を小数点形式に変換して入力：
  - 朝勤 = 9.5
  - 昼勤 = 12
  - 夜勤 = 13.5
- 希望休／有給はセル空欄
- 名前不一致は警告表示

#### 自動色付け
- `xlsx` → `xlsx-js-style` に切り替えてスタイル書き込みに対応
- 色分けルール：
  - 希望休 = 赤 (#FF0000)
  - 有給 = ピンク (#FF99CC)
  - 時間変更・その他 = 黄 (#F3EC63)
- 時間セル上段3行＋計算セル下段3行に同色適用

---

## 開発タイムライン

### 5月4日 — プロジェクト発端
- ユーザーから「シフト作成アプリのチャットなかった？」と問い合わせ
- 過去履歴に該当なし、新規セッションとして開発スタート

### 5月5日 — 配布形態の検討
- 「このままデスクトップアプリにする方法ある？」との要望
- Electron / Tauri を選択肢として提示

### 5月6日 — Web版への大転換と基礎機能の集中実装
- **共有方法の決定**：LINE共有要望から、URL共有（GitHub Pages）方式へ
- **localStorage問題発覚** → Firebase + GitHub Pages 構成に決定
- **インフラ構築**：GitHubアカウント作成（最終的に MARUNAGE-creator）、リポジトリ shift-system 作成、Firebaseプロジェクト shift-system 作成（Firestore asia-northeast1）、GitHub Pages 公開
- **教訓**：Chromeで開いてCtrl+S保存はNG。ダウンロードファイルを直接GitHubへドラッグ
- **UI改修ラッシュ**：
  - 時間変更ダイヤル（9-23時／15分刻み、シフト種別選択中でも開く）
  - タップサイクル → 日付タップでポップアップ式に変更
  - カレンダーマス内の時間表示を2行化
  - 提出ボタンを赤・大型・パルスアニメ化、メモ欄追加
  - 🌴 有給休暇ボタン追加（紫）
  - 凡例カラフルチップ化、ナビ視認性向上
  - 時間帯選択必須化（未選択時はカレンダー非表示・提出タブ🔒ロック）
  - スタッフ長押しドラッグ並び替え（Firestore sortOrder保存）
- **Excel関連**：既存テンプレート反映機能、自動色付け（xlsx-js-style導入）
- **取扱説明書**：全21枚（スタッフ編11枚＋管理者編10枚、PPT＋PDF）

### 5月8日 — 締め切り機能と給与計算Phase 1
- **締め切り機能**：管理者「設定」タブ新設、ON/OFF・日付・カスタムメッセージ・プレビュー、翌月1日自動リセット、Firestore `settings/global`
- **締め切りバナー**：ログイン画面とカレンダー画面に常時表示。残日数で色変化・点滅、「【5月15日(木)】まで」表記
- **給与計算の壁打ち**：管理者のみ閲覧／Excel再アップロード方式／詳細計算（残業+25%・深夜+25%・両方+50%）／個別時給／6h以上で1h休憩控除／カレンダー月締め／月別保存
- **給与計算 Phase 1 実装完了**：時給フィールド追加、💰給与計算タブ新設、Excelアップロード→集計→内訳表示、`salaryCalcs/{ym}` 月別保存、複数テストケース検証済み
- Phase 2 残タスク：週40時間判定、法定休日割増+35%

### 5月13日 — ドキュメント整備
- 技術仕様書 v2 作成（全22ページ：概要5＋スタッフ機能5＋管理者機能7＋仕様詳細5）
  - Firestoreデータ構造、給与計算ロジック疑似コード、技術スタック、Phase 2 ロードマップ
- デスクトップ「シフトシステム」フォルダに index.html・仕様書 v2（PDF/PPTX）・取扱説明書（PDF/PPTX）を集約

### 6月5日 — 運用上のトラブル対応と可視化改善
- **Excel文字化け問題**：`bookSST: true` / `compression: true` を追加。化け箇所の特定が残課題
- **Firebaseテストモード30日期限切れ**：管理画面のスタッフ追加が不可に。Firestoreルールを `allow read, write: if true;` に書き換え。公開アクセスへの不安に対し、URL社内限定・機密情報なしとして説明。将来オプションとして合言葉方式／Firebase Auth を提案
- **データ有無の可視化**：データ出力タブの月セレクタ下にステータスバッジを追加（提出済み人数、反映できる希望データ件数、給与計算済み件数と更新日時を一目で判別）

---

## 運用ノウハウ（落とし穴と日次・月次フロー）

### ファイル更新フロー（最重要）
- **Chromeで開いて Ctrl+S 保存は厳禁**
  - Firebase config が消える、`.mhtml` 形式で保存されるなどのトラブルが発生
  - 公開URL（https://MARUNAGE-creator.github.io/shift-system/）から表示確認するのはOK、しかし「保存し直す」のはNG
- **正しい手順**: ダウンロードフォルダにある `index.html` をそのまま GitHub（https://github.com/MARUNAGE-creator/shift-system/upload/main）に直接ドラッグ＆ドロップ

### Firestore セキュリティルール
- 現在 `allow read, write: if true;` で運用中（フルオープン）
- **この設定にした経緯**: 初期の 30日テストモード（`if request.time < timestamp.date(2026, 6, 5);`）が 6/5 に期限切れし、管理画面でスタッフ追加ができなくなったため
- **許容している理由**:
  - URLは社内限定で外部に拡散していない
  - 機密情報（個人番号・給与口座等）は保存していない
  - 実用上の支障なし
- **リスク**: URLが流出すれば誰でも読み書きできる。スタッフ名・シフト・時給は閲覧/改ざん可能
- ユーザーは「怖いんだけど」と不安を表明 → セキュリティ強化策（後述）は要検討案件

### 月初の自動動作
- 提出締め切り機能は **翌月1日に自動リセット**
- `settings/global` ドキュメントで「締め切りON/OFF・日付（1-28）・カスタムメッセージ」を管理
- 締め切り当日が近づくとバナーが色変化（6日以上=青 / 3-5日=黄 / 1-2日・当日=赤＋点滅）

### その他の運用ノウハウ
- Excelスタイル付き出力には `xlsx` ではなく **`xlsx-js-style`** が必須（標準xlsxはスタイル書き込み不可）
- Excelテンプレート反映時、スタッフ名は半角/全角空白を自動正規化して照合
- 取扱説明書（PowerPoint/PDF・全21枚）と仕様書 v2（全22ページ）はデスクトップ「シフトシステム」フォルダに保管

---

## 残課題・Phase 2 TODO

### 給与計算 Phase 2 残タスク
- **週40時間判定**: 木曜起算（木〜水締め）で週40時間超過分を残業扱いにする
- **法定休日割増 +35%**: 現在は残業25%＋深夜25%（22-24時）のみ実装。法定休日割増は未対応で Phase 2 に持ち越し

### セキュリティ強化案（将来オプション）
- **合言葉方式**: アプリ起動時にパスワード入力 → 通過したら Firestore アクセス
- **Firebase Authentication**: 正式な認証基盤に切替え、ユーザー単位でアクセス制御
- いずれもユーザーの「if true 不安」への対応策として候補化済み

### Excel文字化け問題（要追跡）
- 6/5 報告: エクセル出力時に文字化け発生
- 暫定対策: `bookSST: true` / `compression: true` をオプション追加で対応
- **未確認事項**: 化け箇所が「ファイル名 / セル中身 / シート名」のどれかが未特定 → 再発時に切り分けが必要

### その他
- 不一致スタッフ名はテンプレ反映時に警告表示されるが、運用が長期化すると名寄せ作業が増える可能性あり

---

## ユーザー特性（好み・コミュニケーションパターン）

### UI設計の好み
- **ポップアップ式 > タップサイクル切替**: 「タップで切替がわかりづらい」→ 日付タップで大きなポップアップを開く方式を採用
- **視認性を最優先**: 色分け（希望休=赤 / 有給=ピンク / 時間変更=黄）、大きさ、太字、点滅アニメーションを多用
  - 例: 提出ボタンは赤＋大型＋パルスアニメ＋「▼ 入力が終わったらここをタップ ▼」
- ステータスは「ぱっと見」で判別したい
  - 例: 月セレクタ下にバッジで「✅ 提出済み 12名 / ⚪ まだありません」
- 日付表記は「【5月15日(木)】まで」のように **【】＋曜日** をセットで欲しがる

### コミュニケーションスタイル
- **壁打ち → 論点整理 → 実装依頼** の順で進めるのを好む
  - 給与計算機能では「①誰が見る ②計算データ ③精度 ④時給」などQ&A形式で設計を固めた
- **ステップバイステップで丁寧に**教えてほしい（GitHubアップロード時にも一手順ずつガイドが必要）
- 詳細な技術論より **「動くこと」** を優先
- **セキュリティ警告に強い不安**を示す（「怖いんだけど」「こんなんなるなら最初から教えてよ」）→ 丁寧な説明と「実害があるか／ないか」の判断材料を必ず添える

### 提案受け取りのクセ
- ユーザーの素案をそのまま実装するのではなく、「より良い別案」を提示するとよく採用される
- プライバシー意識あり（職場でプライベートアドレスを使うのを嫌い、GitHubユーザー名を MARUNAGE-creator に変更した経緯）

---

## 次のアクション候補

- **Excel文字化けの切り分け調査**: 次回発生時に「ファイル名／セル中身／シート名」のどこで化けているかをスクリーンショット付きで特定し、xlsx-js-style 側のエンコード設定を再検討する
- **給与計算 Phase 2 着手**: 週40時間判定（木〜水締め）と法定休日割増+35%を実装し、Phase 1 のテストケースに加えて週またぎ・祝日パターンの検証を追加
- **Firestore セキュリティ強化の意思決定**: 合言葉方式（軽量・短期）と Firebase Authentication（本格・長期）を比較表にして提示し、ユーザーの不安解消を兼ねてどちらに進むか決定
- **運用1ヶ月レビュー**: 6月分のシフト提出〜給与計算が一巡したタイミングで、現場スタッフ・管理者双方からフィードバックを集め、UI改善・名寄せ運用の整理に反映

---

参照元: `C:\Users\shinc\Desktop\shift-system\.dev-chat-log.md`
